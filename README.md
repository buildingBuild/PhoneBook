Contact/Phone Book that reads in various contact information stores it using maps and pairs(data structures)
saves the information into a csv file which is then converted to a sql document

Key Functions -:
// not sure for now
